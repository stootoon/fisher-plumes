compute:
  fit_b: '[True]'
  fit_k: '[False]'
  istart: 1000
  window_length: '[0.5*SEC, 1*SEC, 2*SEC]'
  window_shape: '[("boxcar"), ("hann"), ("kaiser",9), ("kaiser",16)]'
  integral_length_scale_locs_absolute__: '[(0,0) * M, "PROBE_0"]'
  dmax_um: '["1 * PITCH"]'
init:
  freq_max: 20 * HZ
  n_bootstraps: 50
  pairs_mode: signed
  py_mode: absolute
  random_seed: 0
  sim_name: n16Tslow
  which_coords: '[(1.05,0.5) * M]'
